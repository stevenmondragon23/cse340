<% if (messages()) { %>
  <%= messages() %>
<% } %>
<% if (errors) { %>
  <ul class="notice">
 <% errors.array().forEach(error => { %>
   <li><%= error.msg %></li>
<%  }) %>
 </ul>
<% } %>


<h1><%= title %></h1>

<form class="form-inv" method="post" action="/account/login">

    <label for="account_email">Email</label>
    <input
        type="email"
        name="account_email"
        id="account_email"
        required
        placeholder="Enter your email address"
    >

    <label for="account_password">Password</label>

    <div class="password-rules">
        Passwords must be at least 12 characters long and include
        1 uppercase letter, 1 number, and 1 special character.
    </div>

    <input
        type="password"
        name="account_password"
        id="account_password"
        required
        pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$"
    >

        </p>
    <button type="submit">Login</button>
        <p class="pop-messa">
        Don't have an account?
        <a href="/account/register">Register here</a>
</form>
